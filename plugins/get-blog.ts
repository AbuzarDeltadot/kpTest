export default defineNuxtPlugin(async () => {
  // const blogStore = useBlogStore()
  // const currentRount = useRoute()
  // const slug = currentRount.params.slug
  // await blogStore.getBlog(slug)
})
